apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "fileservice.fullname" . }}
  labels:
    app: {{ include "fileservice.name" . }}
spec:
  replicas: {{ .Values.replicaCount }}
  selector:
    matchLabels:
      app: {{ include "fileservice.name" . }}
  template:
    metadata:
      labels:
        app: {{ include "fileservice.name" . }}
    spec:
      containers:
        - name: {{ include "fileservice.name" . }}
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          ports:
            - containerPort: {{ .Values.service.targetPort }}
          resources:
            {{- toYaml .Values.resources | nindent 12 }}
