apiVersion: v1
kind: Service
metadata:
  name: {{ include "fileservice.fullname" . }}-service
spec:
  type: {{ .Values.service.type }}
  selector:
    app: {{ include "fileservice.name" . }}
  ports:
    - protocol: TCP
      port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
