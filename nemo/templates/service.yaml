apiVersion: v1
kind: Service
metadata:
  name: {{ include "rydercupva-guardrails.fullname" . }}
spec:
  type: {{ .Values.service.type }}
  selector:
    app: {{ include "rydercupva-guardrails.name" . }}
  ports:
    - protocol: TCP
      port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
