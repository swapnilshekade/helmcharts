apiVersion: v1
kind: Service
metadata:
  name: {{ include "RydercupVA-graphql.fullname" . }}-service
spec:
  type: {{ .Values.service.type }}
  selector:
    app: {{ include "RydercupVA-graphql.name" . }}
  ports:
    - protocol: TCP
      port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
